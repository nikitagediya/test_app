<h1><%= @post.title %></h1>
<small><%= @post.created_at.strftime("%b %d. %y")%></small>
<p><%= @post.body %></p>

<%= link_to "Edit Post", edit_post_path%> | <%= button_to "Delete", @post, method: :delete %>